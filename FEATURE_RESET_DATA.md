# 🔄 ميزة حذف البيانات وإعادة الرفع

## 📋 المشكلة التي تم حلها

عند رفع نفس الملف مرتين (مثل P&L)، كانت البيانات تتكرر وتسبب مشاكل في التحليل.

## ✅ الحل المُطبَّق

تم إضافة **زر "حذف البيانات القديمة وإعادة الرفع"** لكل نوع من البيانات:

### 1️⃣ بيانات الطاقة (Capacity)

**الموقع:** مركز البيانات → تبويب "بيانات الطاقة"

**كيفية الاستخدام:**
1. إذا رفعت ملف طاقة سابقاً، سيظهر تحذير: ⚠️ "يوجد بيانات طاقة محملة مسبقاً"
2. اضغط على زر 🗑️ "حذف البيانات القديمة وإعادة الرفع"
3. سيتم حذف:
   - ✅ البيانات من الذاكرة
   - ✅ البيانات من قاعدة البيانات
   - ✅ جميع حالات الحفظ
4. يمكنك الآن رفع ملف جديد بدون تكرار

### 2️⃣ بيانات P&L (الأرباح والخسائر)

**الموقع:** مركز البيانات → تبويب "بيانات P&L"

**كيفية الاستخدام:**
1. إذا رفعت ملف P&L سابقاً، سيظهر تحذير: ⚠️ "يوجد بيانات P&L محملة مسبقاً"
2. اضغط على زر 🗑️ "حذف البيانات القديمة وإعادة الرفع"
3. سيتم حذف:
   - ✅ البيانات من الذاكرة
   - ✅ هوامش الربح المحسوبة
   - ✅ البيانات من قاعدة البيانات
4. يمكنك الآن رفع ملف جديد محدّث

### 3️⃣ بيانات الطلبات (Orders/Fulfillment)

**الموقع:** مركز البيانات → تبويب "بيانات الطلبات"

**كيفية الاستخدام:**
1. إذا رفعت ملفات طلبات سابقاً، سيظهر تحذير: ⚠️ "يوجد بيانات طلبات محملة مسبقاً"
2. اضغط على زر 🗑️ "حذف البيانات القديمة وإعادة الرفع"
3. سيتم حذف:
   - ✅ بيانات الطلبات
   - ✅ التحليل الإقليمي
   - ✅ بيانات المنافسين (المستخرجة تلقائياً)
   - ✅ بيانات العملاء (المستخرجة تلقائياً)
   - ✅ بيانات المبيعات (المستخرجة تلقائياً)
   - ✅ بيانات الموسمية (المستخرجة تلقائياً)
4. يمكنك الآن رفع ملفات جديدة

### 4️⃣ بيانات الموردين (Suppliers)

**الموقع:** مركز البيانات → تبويب "بيانات الموردين"

**كيفية الاستخدام:**
1. إذا رفعت ملف موردين سابقاً، سيظهر تحذير: ⚠️ "يوجد بيانات موردين محملة مسبقاً"
2. اضغط على زر 🗑️ "حذف البيانات القديمة وإعادة الرفع"
3. سيتم حذف:
   - ✅ بيانات الموردين من الذاكرة
   - ✅ بيانات الموردين من قاعدة البيانات
4. يمكنك الآن رفع ملف موردين جديد

---

## 🎯 متى تستخدم هذه الميزة؟

### ✅ استخدمها عند:

1. **تحديث البيانات الشهرية**
   - مثال: رفع P&L الشهر الجديد بدلاً من القديم

2. **تصحيح أخطاء في الملف**
   - مثال: اكتشفت خطأ في ملف الطاقة ورفعته مرتين بالخطأ

3. **تغيير فترة التحليل**
   - مثال: تريد تحليل بيانات 6 أشهر بدلاً من 3 أشهر

4. **تحديث بيانات الموردين**
   - مثال: إضافة موردين جدد أو تحديث الأسعار

### ❌ لا تستخدمها عند:

1. **إضافة بيانات جديدة لنفس الفترة**
   - في هذه الحالة، البيانات ستُضاف تلقائياً

2. **أول مرة ترفع البيانات**
   - الزر لن يظهر أصلاً!

---

## 🔧 التفاصيل التقنية

### الكود المُضاف:

```python
# مثال: زر حذف بيانات P&L
if st.session_state.data_loaded.get('pnl', False):
    st.warning("⚠️ يوجد بيانات P&L محملة مسبقاً")
    if st.button("🗑️ حذف البيانات القديمة وإعادة الرفع", type="secondary", key="reset_pnl"):
        # حذف من session state
        st.session_state.data_loaded['pnl'] = False
        st.session_state.pnl_analyzed = False
        
        # حذف من المحرك
        engine.pnl_data = None
        engine.profit_margins = {}
        
        # حذف من قاعدة البيانات
        db.delete_table('pnl')
        
        st.success("✅ تم حذف البيانات القديمة. يمكنك الآن رفع ملف جديد.")
        st.rerun()
```

### الأماكن المُعدَّلة:

| الملف | الدالة | الأسطر المضافة |
|-------|--------|----------------|
| `app_v2.py` | `show_data_hub()` | ~60 سطر |

### الجداول المحذوفة عند الضغط:

| نوع البيانات | الجداول المحذوفة |
|--------------|-------------------|
| **Capacity** | `capacity` |
| **P&L** | `pnl` |
| **Orders** | `orders`, `competitors`, `customers`, `sales_history`, `seasonality` |
| **Suppliers** | `suppliers` |

---

## 🎨 شكل الزر في الواجهة

```
⚠️ يوجد بيانات P&L محملة مسبقاً

┌─────────────────────────────────────────────┐
│  🗑️ حذف البيانات القديمة وإعادة الرفع      │
└─────────────────────────────────────────────┘
```

**اللون:** رمادي (secondary)  
**الموقع:** قبل مربع رفع الملف مباشرة

---

## ✅ الفوائد

1. ✅ **منع التكرار:** لا مزيد من البيانات المكررة
2. ✅ **سهولة التحديث:** تحديث البيانات بضغطة زر واحدة
3. ✅ **أمان:** تحذير واضح قبل الحذف
4. ✅ **شمولية:** يحذف من كل الأماكن (ذاكرة + قاعدة بيانات)
5. ✅ **إعادة تحميل تلقائية:** الصفحة تُحدّث تلقائياً بعد الحذف

---

## 📝 مثال عملي

### السيناريو:
رفعت ملف P&L للشهر 3، ثم رفعته مرة أخرى بالخطأ.

### قبل الميزة:
❌ البيانات تتكرر  
❌ الإيرادات تظهر مضاعفة  
❌ هامش الربح خاطئ  
❌ تحتاج إعادة تشغيل التطبيق

### بعد الميزة:
✅ تحذير يظهر: "يوجد بيانات P&L محملة مسبقاً"  
✅ تضغط زر "حذف البيانات القديمة"  
✅ تظهر رسالة نجاح  
✅ ترفع الملف الصحيح  
✅ كل شيء يعمل بشكل صحيح!

---

## 🚀 كيفية الاستخدام الموصى بها

### خطوات أفضل ممارسة:

1. **تحقق من البيانات قبل الرفع**
   - افتح الملف وتأكد من صحته

2. **ارفع الملف مرة واحدة**
   - لا تضغط "رفع" مرتين

3. **إذا رفعت بالخطأ:**
   - ✅ اضغط زر "حذف البيانات القديمة"
   - ✅ ارفع الملف الصحيح

4. **للتحديث الشهري:**
   - ✅ احذف بيانات الشهر القديم
   - ✅ ارفع بيانات الشهر الجديد

---

## 🔒 الأمان

- ✅ **لا يحذف إلا بعد تأكيد:** يجب الضغط على الزر
- ✅ **تحذير واضح:** رسالة تحذير قبل الحذف
- ✅ **رسالة نجاح:** تأكيد بعد الحذف
- ✅ **لا يمكن التراجع:** الحذف نهائي (تأكد قبل الضغط!)

---

## 📞 دعم إضافي

إذا واجهت أي مشكلة:

1. جرب إعادة تحميل الصفحة (F5)
2. تأكد من الضغط على زر الحذف قبل رفع الملف الجديد
3. راجع قسم "جداول قاعدة البيانات" في أسفل صفحة "مركز البيانات"

---

**آخر تحديث:** 17 نوفمبر 2025  
**الإصدار:** V2.1  
**الحالة:** ✅ مُفعَّل ويعمل
