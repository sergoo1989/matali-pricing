# 🎉 التحديث الجديد: نظام الحفظ الدائم وعرض السعر المتقدم

## ✨ الميزات الجديدة

### 1. 💾 نظام الحفظ الدائم (Database Manager)

**المشكلة السابقة:**
- كان يجب رفع جميع الملفات في كل مرة تفتح فيها البرنامج
- فقدان البيانات عند إغلاق التطبيق
- عدم القدرة على حفظ العروض بشكل دائم

**الحل الجديد:**
- ✅ حفظ تلقائي لجميع البيانات في قاعدة بيانات SQLite
- ✅ تحميل تلقائي عند فتح البرنامج
- ✅ حذف البيانات يدوياً فقط عند الحاجة
- ✅ نسخ احتياطية قابلة للتصدير

**البيانات المحفوظة:**
- بيانات الطاقة (Capacity)
- بيانات P&L
- بيانات الطلبات (Orders)
- بيانات الموردين (Suppliers)
- عروض الأسعار (Quotes)
- الإعدادات (Settings)

### 2. 🧾 نظام عرض السعر المتقدم

**نموذجين للتسعير:**

#### أ) التسعير اليدوي 📝
- جدول خدمات كامل قابل للتعديل
- خصومات تلقائية حسب تصنيف العميل (Standard/Preferred/Strategic)
- 3 فئات خدمات: التجهيز والتخزين، الشحن، الخدمات الإضافية
- حساب السعر الشامل لكل طلب
- تحليل الربحية بناءً على التكلفة الفعلية

#### ب) التسعير الذكي 🤖
- تسعير تلقائي بناءً على بيانات P&L والطاقة
- حساب التكلفة الفعلية لكل طلب
- تحديد هامش الربح المستهدف
- توقعات الإيرادات والأرباح الشهرية
- تحليل استغلال الطاقة

**مميزات إضافية:**
- ✅ حفظ العروض في قاعدة البيانات
- ✅ مقارنة الأسعار قبل وبعد الخصم
- ✅ ملخص التكاليف الشهرية
- ✅ رسوم بيانية تفاعلية
- ✅ تكامل كامل مع بيانات النظام

### 3. 🗄️ صفحة إدارة قاعدة البيانات

تاب جديد في "مركز البيانات" يوفر:
- 📊 معلومات قاعدة البيانات (الحجم، عدد الجداول، آخر تحديث)
- 📋 قائمة الجداول المحفوظة مع تفاصيلها
- 🗑️ حذف جداول فردية
- 📜 سجل آخر التغييرات
- 💾 نسخ احتياطية قابلة للتحميل
- 🗑️ حذف جميع البيانات (مع تأكيد)

## 🚀 كيفية الاستخدام

### التشغيل الأول:
```bash
cd "c:\Users\ahmed\vs code\PRICE\matali_pricing_system"
streamlit run app_v2.py
```

### سير العمل:
1. **مركز البيانات** → رفع البيانات (مرة واحدة فقط!)
2. **عرض سعر متقدم** → اختر يدوي أو ذكي
3. **حفظ العرض** → يُحفظ تلقائياً في قاعدة البيانات
4. **سجل العروض** → مراجعة جميع العروض المحفوظة

### إدارة البيانات:
- **الحذف:** مركز البيانات → تاب "إدارة قاعدة البيانات" → حذف
- **نسخ احتياطي:** مركز البيانات → تاب "إدارة قاعدة البيانات" → إنشاء نسخة احتياطية

## 📁 الملفات الجديدة

```
matali_pricing_system/
├── database_manager.py          # مدير قاعدة البيانات
├── new_quote_system.py          # نظام عرض السعر الجديد
├── data/                        # مجلد قاعدة البيانات (auto-created)
│   ├── matali_pricing.db        # قاعدة البيانات الرئيسية
│   └── backup_*.sql             # النسخ الاحتياطية
└── app_v2.py                    # التطبيق المحدث

```

## 🔧 التعديلات على الملفات الموجودة

### app_v2.py:
- ✅ إضافة استيراد DatabaseManager و new_quote_system
- ✅ تهيئة قاعدة البيانات عند بدء التطبيق
- ✅ تحميل تلقائي للبيانات المحفوظة
- ✅ حفظ تلقائي عند رفع البيانات
- ✅ تاب جديد "إدارة قاعدة البيانات"
- ✅ صفحة جديدة "عرض سعر متقدم"

## 💡 نصائح الاستخدام

1. **أول مرة:** ارفع جميع بياناتك (Capacity, P&L, Orders, Suppliers)
2. **المرات التالية:** البيانات محملة تلقائياً!
3. **التحديث:** ارفع ملف جديد فقط عند تحديث البيانات
4. **النسخ الاحتياطي:** اعمل نسخة احتياطية أسبوعياً
5. **الحذف:** احذف البيانات القديمة عند الحاجة فقط

## 🎯 الفوائد

### قبل:
- ❌ رفع ملفات في كل مرة
- ❌ فقدان البيانات عند الإغلاق
- ❌ عروض أسعار غير محفوظة
- ❌ عمليات يدوية متكررة

### بعد:
- ✅ رفع مرة واحدة فقط
- ✅ بيانات دائمة ومحفوظة
- ✅ عروض محفوظة في قاعدة بيانات
- ✅ نظام تلقائي ذكي

## 📞 الدعم

للاستفسارات أو المشاكل، راجع:
- `DATABASE_SYSTEM.md` - توثيق كامل لنظام قاعدة البيانات
- `SUPPLIERS_INTEGRATION.md` - دليل الموردين
- `app_v2.py` - الكود المصدري

---

**تاريخ التحديث:** 17 نوفمبر 2025  
**الإصدار:** 2.1.0  
**الميزات الجديدة:** 3 (Database, New Quote, DB Management Tab)
