# 📦 تكامل نظام الموردين الشامل

## 📋 نظرة عامة

تم دمج نظام إدارة الموردين وشركات الشحن الخارجية في النظام، مما يتيح:
- إدارة بيانات الموردين
- مقارنة أسعار الشحن تلقائياً
- حساب تكاليف التجهيز الخارجي (3PL)
- اختيار أفضل مورد لكل طلب

## 🎯 الملفات المضافة/المُعدّلة

### ✅ ملف جديد: `supplier_data_processor.py`
**الوظيفة:** معالج بيانات الموردين

**الكلاسات:**
```python
class SupplierDataProcessor:
    - load_suppliers()
    - calculate_shipping_cost()
    - get_best_shipping_supplier()
    - calculate_fulfillment_cost()
    - get_outsourcing_options()
    - compare_suppliers()
    - add_supplier()
    - save_suppliers()
```

**الميزات:**
- ✅ حساب تلقائي لتكلفة الشحن حسب المدينة والوزن
- ✅ دعم رسوم COD، الشبكة، الوزن الإضافي
- ✅ اختيار أفضل مورد (أقل تكلفة)
- ✅ تحديد موردي التجهيز الخارجي
- ✅ مقارنة شاملة بين الموردين

### ✅ app_v2.py - التحديثات

#### 1. قالب Excel جديد للموردين - السطر 142
```python
suppliers_template = pd.DataFrame({
    'supplier_name': [...],
    'service_type': ['shipping', 'fulfillment', 'storage', 'VAS'],
    'city_range': ['الرياض', 'خارج الرياض', 'كل المملكة'],
    'base_price': [...],
    'cod_fee': [...],
    'network_fee': [...],
    'weight_limit': [...],
    'extra_kg_price': [...],
    'is_fulfillment_provider': ['yes', 'no']
})
```

#### 2. زر تحميل القالب - السطر 258
```python
st.download_button(
    label="📦 قالب بيانات الموردين",
    data=templates['suppliers'],
    file_name="template_suppliers_data.xlsx"
)
```

#### 3. تبويب جديد (Tab 7) - السطر 339
```python
tab1, tab2, tab3, tab4, tab5, tab6, tab7 = st.tabs([
    ...
    "📦 بيانات الموردين"
])
```

#### 4. واجهة تحميل الموردين - السطر 833
**المكونات:**
- تعليمات مفصلة (expanded=True)
- رفع ملف Excel/CSV
- عرض البيانات المحملة
- 4 مقاييس: موردو الشحن، التجهيز، التخزين، Outsourcing
- مقارنة سريعة لأسعار الشحن
- إضافة مورد يدوياً

### ✅ unified_pricing_engine.py - التحديثات

#### 1. إضافة خاصية suppliers_data - السطر 67
```python
self.suppliers_data = None  # بيانات الموردين
self.supplier_comparison = {}  # مقارنة الموردين
```

#### 2. دالة integrate_suppliers_data() - السطر 234
```python
def integrate_suppliers_data(self, suppliers_df):
    self.suppliers_data = suppliers_df
    self.supplier_comparison = self._analyze_suppliers()
    return self
```

#### 3. دالة _analyze_suppliers() - السطر 247
**التحليلات:**
- عدد الموردين لكل نوع خدمة
- متوسط/أقل/أعلى سعر
- توفر التجهيز الخارجي
- قائمة بأسماء الموردين

#### 4. تحديث _calculate_shipping_cost() - السطر 835
**المنطق الجديد:**
```python
# أولوية 1: أسعار الموردين
if self.suppliers_data exists:
    processor = SupplierDataProcessor(self.suppliers_data)
    best_supplier = processor.get_best_shipping_supplier(...)
    return best_supplier['total_cost'] * 1.25  # هامش 25%

# أولوية 2: بيانات الطلبات التاريخية
elif city in self.regional_analysis:
    # الكود القديم

# افتراضي
else:
    return 25
```

## 📊 هيكل ملف الموردين

### الأعمدة المطلوبة

| العمود | النوع | الوصف | مثال |
|--------|------|-------|------|
| supplier_name | نص | اسم المورد | "شركة الشحن السريع" |
| service_type | نص | shipping, fulfillment, storage, VAS | "shipping" |
| city_range | نص | الرياض / خارج الرياض / كل المملكة | "كل المملكة" |
| base_price | رقم | السعر الأساسي بالريال | 15.0 |
| cod_fee | رقم | رسوم الدفع عند الاستلام | 3.0 |
| network_fee | رقم | رسوم الشبكة | 2.0 |
| weight_limit | رقم | الوزن المسموح بالكيلو | 5.0 |
| extra_kg_price | رقم | سعر الكيلو الإضافي | 3.0 |
| is_fulfillment_provider | نص | yes / no | "yes" |
| notes | نص | ملاحظات (اختياري) | "توصيل سريع" |

### مثال على البيانات

```excel
┌───────────────────────┬──────────────┬────────────────┬────────────┬─────────┬─────────────┬──────────────┬──────────────────┬─────────────────────────┐
│ supplier_name         │ service_type │ city_range     │ base_price │ cod_fee │ network_fee │ weight_limit │ extra_kg_price   │ is_fulfillment_provider │
├───────────────────────┼──────────────┼────────────────┼────────────┼─────────┼─────────────┼──────────────┼──────────────────┼─────────────────────────┤
│ شركة الشحن السريع    │ shipping     │ الرياض         │ 15.0       │ 3.0     │ 2.0         │ 5.0          │ 3.0              │ no                      │
│ شركة التوصيل الذهبي   │ shipping     │ كل المملكة     │ 18.0       │ 2.5     │ 1.5         │ 5.0          │ 2.5              │ no                      │
│ 3PL للتجهيز          │ fulfillment  │ كل المملكة     │ 5.5        │ 0.0     │ 0.0         │ 0.0          │ 0.0              │ yes                     │
│ شركة المستودعات       │ storage      │ الرياض         │ 3.0        │ 0.0     │ 0.0         │ 0.0          │ 0.0              │ yes                     │
└───────────────────────┴──────────────┴────────────────┴────────────┴─────────┴─────────────┴──────────────┴──────────────────┴─────────────────────────┘
```

## 🔄 آلية عمل النظام

### 1️⃣ تحميل بيانات الموردين
```
المستخدم → رفع Excel/CSV → قراءة البيانات → عرض الجدول
       → حفظ تلقائي → engine.integrate_suppliers_data(df)
       → تحليل الموردين → supplier_comparison
```

### 2️⃣ حساب تكلفة الشحن
```
طلب جديد (المدينة، الوزن، القيمة، COD)
       ↓
processor.calculate_shipping_cost()
       ↓
لكل مورد → حساب:
  - السعر الأساسي
  - رسوم COD (إن وجدت)
  - رسوم الشبكة
  - رسوم الوزن الإضافي (إذا تجاوز الحد)
       ↓
الإجمالي لكل مورد
       ↓
processor.get_best_shipping_supplier()
       ↓
أقل تكلفة × هامش ربح (1.25) = السعر النهائي
```

### 3️⃣ التجهيز الخارجي (Outsourcing)
```
عميل يطلب تجهيز خارجي
       ↓
processor.get_outsourcing_options()
       ↓
فلترة: is_fulfillment_provider == 'yes'
       ↓
عرض الخيارات المتاحة + الأسعار
       ↓
اختيار المورد → حساب التكلفة
```

## 💡 حالات الاستخدام

### Case 1: مقارنة موردي الشحن
```python
from supplier_data_processor import SupplierDataProcessor

processor = SupplierDataProcessor(suppliers_df)

# مقارنة شاملة
comparison = processor.compare_suppliers(
    service_type='shipping',
    city='الرياض',
    weight=7,  # كجم
    order_value=500
)

print(comparison)
```

**النتيجة:**
```
┌────────────────────────┬──────────────┬───────────┬──────────────┬───────────────┬──────────┐
│ المورد                 │ السعر الأساسي│ رسوم COD  │ رسوم الشبكة │ رسوم الوزن    │ الإجمالي │
├────────────────────────┼──────────────┼───────────┼──────────────┼───────────────┼──────────┤
│ شركة الشحن السريع     │ 15.0         │ 3.0       │ 2.0          │ 6.0           │ 26.0     │
│ شركة التوصيل الذهبي    │ 18.0         │ 2.5       │ 1.5          │ 5.0           │ 27.0     │
└────────────────────────┴──────────────┴───────────┴──────────────┴───────────────┴──────────┘
أفضل مورد: شركة الشحن السريع (26.0 ر.س)
```

### Case 2: التسعير الديناميكي
```python
# في unified_pricing_engine.py
price_details = engine.calculate_comprehensive_price(
    service_type='shipping',
    city='جدة',
    weight=10,
    order_value=800,
    payment_method='POSTPAID'
)

# النظام تلقائياً:
# 1. يبحث في suppliers_data
# 2. يحسب تكلفة كل مورد
# 3. يختار الأرخص
# 4. يضيف هامش ربح 25%
```

### Case 3: تقرير وفورات التكلفة
```python
# قبل استخدام الموردين
internal_cost = 30  # تكلفة داخلية

# بعد استخدام الموردين
supplier_cost = 26  # من أفضل مورد
final_price = supplier_cost * 1.25  # 32.5

# الوفورات
savings = internal_cost - supplier_cost  # 4 ر.س
savings_percent = (savings / internal_cost) * 100  # 13.3%
```

## 📈 الفوائد

| الفائدة | التأثير |
|---------|---------|
| **تحسين التسعير** | أسعار تنافسية بناءً على أفضل الموردين |
| **وفورات التكلفة** | اختيار أرخص مورد لكل طلب |
| **المرونة** | إمكانية التجهيز الخارجي عند الحاجة |
| **الشفافية** | مقارنة واضحة بين جميع الخيارات |
| **الأتمتة** | اختيار تلقائي للمورد الأفضل |

## 🔧 التكامل مع باقي النظام

### مع محرك التسعير
```python
# في calculate_comprehensive_price()
shipping_cost = self._calculate_shipping_cost(city, weight, order_value, payment_method)
# ← يستخدم الموردين تلقائياً
```

### مع العروض
```python
quote_data = {
    'service_type': 'shipping',
    'supplier_used': best_supplier['supplier_name'],
    'supplier_cost': best_supplier['total_cost'],
    'our_price': best_supplier['total_cost'] * 1.25,
    'margin': 0.25
}
```

### مع التحليلات
```python
dashboard = engine.get_analytics_dashboard()

# تضمين معلومات الموردين
dashboard['suppliers'] = {
    'total_count': len(engine.suppliers_data),
    'shipping_suppliers': len(shipping_suppliers),
    'outsourcing_available': True/False,
    'avg_shipping_cost': avg_cost
}
```

## 🚀 الخطوات القادمة

### قريباً
- [ ] تكامل API مباشر مع شركات الشحن
- [ ] تتبع الأداء الفعلي للموردين
- [ ] تقييم جودة الخدمة (SLA)
- [ ] تجديد تلقائي للأسعار

### مستقبلاً
- [ ] AI لتوقع أفضل مورد حسب الظروف
- [ ] تفاوض تلقائي على الأسعار
- [ ] تكامل مع أنظمة ERP
- [ ] Dashboard موردين متقدم

## 📞 كيفية الاستخدام

### الخطوة 1: تحميل القالب
1. افتح النظام
2. اذهب لـ: **📂 مركز البيانات**
3. اضغط: **📦 قالب بيانات الموردين**
4. احفظ الملف

### الخطوة 2: تعبئة البيانات
1. افتح الملف في Excel
2. املأ بيانات الموردين
3. احفظ الملف

### الخطوة 3: رفع البيانات
1. في **مركز البيانات**
2. اذهب لتبويب: **📦 بيانات الموردين**
3. ارفع الملف
4. شاهد التحليل التلقائي

### الخطوة 4: الاستخدام
- النظام سيستخدم بيانات الموردين تلقائياً
- في التسعير، سيختار أفضل مورد
- في العروض، ستظهر تفاصيل المورد المستخدم

## 🐛 استكشاف الأخطاء

### المشكلة: "لم يتم العثور على موردين"
```python
# السبب: الملف فارغ أو الأعمدة خاطئة
# الحل:
1. تأكد من وجود الأعمدة المطلوبة
2. تأكد من صحة البيانات
3. استخدم القالب الجاهز
```

### المشكلة: "التكلفة مرتفعة جداً"
```python
# السبب: الوزن أو القيمة كبيرة
# الحل:
1. راجع بيانات الموردين
2. تحقق من weight_limit
3. تحقق من extra_kg_price
```

### المشكلة: "لا يوجد موردين للمدينة"
```python
# السبب: city_range لا يشمل المدينة
# الحل:
1. أضف مورد بـ city_range = "كل المملكة"
2. أو أضف مورد خاص بالمدينة
```

---
**تاريخ التحديث:** نوفمبر 2025  
**الإصدار:** 2.0  
**الحالة:** ✅ مُفعّل ومختبر  
**الملفات:** 3 ملفات معدلة، 1 ملف جديد
